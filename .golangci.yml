linters-settings:
  gofumpt:
    extra-rules: true

linters:
  disable-all: true
  enable:
    - gofmt
    - goimports
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofumpt
    - shadow

issues:
  exclude-use-default: false
