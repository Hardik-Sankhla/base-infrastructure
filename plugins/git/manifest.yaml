schema_version: "1.0"
name: git
version: "1.0.0"
description: "Installs Git version control system"
compatibility:
  os: [linux, windows, darwin, android]
  arch: [amd64, arm64]
dependencies: []
provides:
  - "runtime.git"
