# Standard Workflow

For every objective, follow this lifecycle strictly:

Idea -> Architecture -> Task Breakdown -> Implementation -> Verification -> CI -> Documentation -> Merge

Do not jump from Idea to Implementation without updating ADRs or Architecture first.
