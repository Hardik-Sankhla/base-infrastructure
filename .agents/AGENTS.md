# Engineering OS Rules

- **Release Gate Rule:** No feature work may begin or continue while the default branch has failing required CI checks. Every release and merge must leave `main` in a fully green state before the next engineering sprint begins.
